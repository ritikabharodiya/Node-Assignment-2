<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= category._id ? 'Edit Category' : 'Add New Category' %></title>
</head>
<body>
  <h1><%= category._id ? 'Edit Category' : 'Add New Category' %></h1>

  <form action="/categories/<%= category._id || '' %>" method="POST">
    <label for="name">Category Name:</label>
    <input type="text" id="name" name="name" value="<%= category.name || '' %>" required>
    
    <button type="submit"><%= category._id ? 'Update' : 'Create' %></button>
  </form>
</body>
</html>
